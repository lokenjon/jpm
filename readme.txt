Run 'mvn clean compile test'

The approach is that there is an incoming feed (webservice) that provides 'instruction' data. 
Statistical data is generated on the fly. The stats are not generated by having to access all data of the day.  

Having thought more of the task at hand, I would not have used json, but rather an array with
all data. In this way it is much easier to do the stats.